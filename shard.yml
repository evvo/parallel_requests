name: parallel_requests
version: 0.1.0

authors:
  - Evtimiy Mihaylov <evo@vaupe.com>

targets:
  parallel_requests:
    main: src/parallel_requests.cr

crystal: 0.35.1

dependencies:
  sqlite3:
    github: crystal-lang/crystal-sqlite3
  micrate:
    github: amberframework/micrate
license: MIT
